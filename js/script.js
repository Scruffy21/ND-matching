const game = {
    cardsOpen = 0,
    cardsAll = 16,
    moves = 0,
    time = 0,
    comparingFinished = false,
    gamefinished = false,
}

const icons = ["tiger", "hippo", "lion", "monkey", "zebra", "crocodile", "antelope", "elephant"];

cards

//call randomiser with icons + icons concatenated

function randomiser(source) { 
    for (let i = 0; i < game.cardsAll; i++) { 

    }
}

function card(source) { 
    this.source = source;
}

function randIndex(range) { 
    
}